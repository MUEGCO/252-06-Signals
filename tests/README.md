# Visible Checks

Run the local visible check with:

```bash
./scripts/test.sh
```

This check compiles the student binary and verifies that the program produces
the exact 10-line output shown in the README. The test also rejects binaries
that still emit `TODO:` placeholder text.

There are no hidden tests beyond what is described here.
